{
  "name": "swap-events-hub",
  "private": true,
  "version": "1.0.0",
  "description": "Swap Events Hub — MERN event booking platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "concurrently -n server,client -c blue,green \"npm run start --prefix server\" \"npm run preview --prefix client\"",
    "start:render": "npm run start --prefix server",
    "build": "npm run build --prefix client",
    "build:render": "npm install --prefix server && npm install --include=dev --prefix client && npm run build --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
